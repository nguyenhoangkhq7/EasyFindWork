{
  "name": "json_server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm run json-server",
    "json-server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
